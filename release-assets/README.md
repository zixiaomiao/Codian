# Release Assets

Download the installer for your system and run it directly:

- macOS: `install-macos.command`
- Windows: `install-windows.cmd`

The installers clone or update `zixiaomiao/codian` directly into `~/.codex/skills/codian` and register Codian in the Codex personal marketplace.
